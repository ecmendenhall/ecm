{% extends "base.j2" %}
{% from "macros.j2" import render_excerpt with context  %}

{% block main %}
<div class="overlap"><h1>{{ node.title }}</h1></div>
    <div class="framed">
        <div class="content">
            <ul class="listing clear">
            {% for res in resource.node.walk_resources_sorted_by_time() %}
                <li>
                {{ render_excerpt(res) }}
                </li>
            {% endfor %}
            </ul>
        </div>
    </div>
</div>
{% endblock main %}
